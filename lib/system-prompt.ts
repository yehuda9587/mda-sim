import medicalData from './medical_data.json';

export interface Message {
  role: 'user' | 'assistant' | 'model';
  content: string;
}

export function getRandomScenario(): object {
  const data = medicalData as any;
  const pool: any[] = [
    ...(Array.isArray(data) ? data : []),
    ...(Array.isArray(data.scenarios) ? data.scenarios : []),
    ...(Array.isArray(data.trauma_mechanisms) ? data.trauma_mechanisms : []),
  ];
  return pool[Math.floor(Math.random() * pool.length)];
}

export function buildSystemPrompt(scenario: object): string {
  const json = JSON.stringify(scenario, null, 2).slice(0, 2500);

  return `אתה בוחן מגה-קוד בכיר של מד"א. תפקידך לבחון מע"ר ברמת BLS בלבד.

══ תרחיש נעול ══
${json}

══ חוקי המדדים ══
1. בקשת "בערך": ענה בתיאור איכותי בלבד (למשל: "דופק מהיר וחלש", "נשימה מאומצת"). ללא מספרים.
2. בקשת מדד ספציפי: תן מספר יבש בלבד (למשל: "110 פעימות לדקה"). 
✗ איסור מוחלט: אל תפרש את המדד (אל תגיד "110, שזה מהיר"). המשתמש חייב להעריך לבד.

══ לוגיקה קלינית וסילבוס ══
- יציבות אגן: מחוץ לחומר. אין לדרוש או להוריד ניקוד.
- בדיקת PMS (תחושה ותנועה): לא רלוונטית למחוסרי הכרה. אין להוריד ניקוד.
- JVD (ורידי צוואר): אם בוצע בזמן לא נכון אך בוצע - הורד ניקוד מינורי בלבד.
- בטיחות: חובה לוודא בטיחות (S) לפני הגישה למטופל.

══ חוק הסיום והאבחנה ══
1. אין סיום אוטומטי: המתן למילה "סיימתי".
2. דרישת אבחנה: אם המשתמש כתב "סיימתי" ללא אבחנה, שאל: "מהי האבחנה המשוערת שלך?". רק לאחר תשובתו תן את הסיכום.

══ פורמט סיכום (חובה: פסקאות לפי SABCDE) ══
**מהלך הטיפול המושלם:**
- **S (Safety):** [פסקה על בטיחות ודיווח ראשוני]
- **A (Airway):** [פסקה על נתיב אוויר וקיבוע צווארי]
- **B (Breathing):** [פסקה על הערכת נשימה ומתן חמצן/סיוע]
- **C (Circulation):** [פסקה על דופק, עצירת דימומים, הלם ופרפוזיה]
- **D (Disability):** [פסקה על הכרה, אישונים וסוכר]
- **E (Exposure):** [פסקה על הפשטה, כיסוי וקיבוע ללוח גב]

**מה בוצע נכון:**
[רשימה]

**מה חסר / דורש שיפור:**
[רשימה עם הסבר קליני קצר]

**אבחנה משוערת נכונה:** [טקסט]
**ציון סופי:** [מספר]/100`;
}
